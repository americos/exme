<h2>New Expense</h2>

<%= form_for @expense do |f| %>
	<p>
		<%= f.label :title %>
		<%= f.text_field :title%>
	</p>
	<p>
		<%= f.label :description %>
		<%= f.text_field :description %>
	</p>
	<%= f.submit "Save" %>
<% end %>